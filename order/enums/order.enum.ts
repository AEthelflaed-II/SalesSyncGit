export enum OrderStatus {
  PENDING = 'pending',
  CREATED = 'created',
  PAID = 'paid',
  CONFIRMED = 'confirmed',
  DELIVERED = 'delivered',
  FINISHED = 'finished',
  CANCELED = 'canceled',
  REFUNDED = 'refunded',
}
