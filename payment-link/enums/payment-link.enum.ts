export enum PaymentLinkStatus {
  CREATED = 'created',
  ACTIVE = 'active',
  INITIATED = 'initiated',
  PROCESSING = 'processing',
  PENDING = 'pending',
  EXPIRED = 'expired',
  PAID = 'paid',
  CANCELED = 'canceled',
  FAILED = 'failed',
}
